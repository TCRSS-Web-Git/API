meta {
  name: Update Password
  type: http
  seq: 6
}

put {
  url: {{backend_url}}/api/v1/user/password
  body: json
  auth: none
}

body:json {
  {
    "current_password": "password",
    "password": "newpassword",
    "password_confirmation": "newpassword"
  }
}
